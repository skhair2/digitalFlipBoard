# Digital FlipBoard - Backend Environment Variables
# SECURITY CRITICAL: Do NOT commit the actual .env file to version control
# Copy this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=development
PORT=3001

# CORS Configuration
# ⚠️ CRITICAL: Must be set in production! Server will fail to start if empty in production.
# Development: Leave empty (defaults to localhost:5173,localhost:3000)
# Production: Comma-separated list of allowed origins (no spaces)
# Example: ALLOWED_ORIGINS=https://flipdisplay.online,https://www.flipdisplay.online
ALLOWED_ORIGINS=

# Supabase Configuration (required for auth)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Redis Configuration (required for sessions and rate limiting)
# Development: redis://localhost:6379
# Production: redis://your-redis-host:6379 or Redis Cloud URL
REDIS_URL=redis://localhost:6379

# Resend Email API Key (PRIVATE - SERVER ONLY)
# 🔴 CRITICAL: This key must NEVER be exposed to the client
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here
FROM_EMAIL=noreply@flipdisplay.online

# Session Management (optional - defaults shown)
# INACTIVITY_TIMEOUT=900000          # 15 minutes in milliseconds
# INACTIVITY_WARNING_THRESHOLD=600000 # 10 minutes in milliseconds
# CHECK_INTERVAL=60000                # 1 minute in milliseconds
# MAX_SESSION_LIFETIME=86400000       # 24 hours in milliseconds

# Logging (optional)
LOG_LEVEL=info
# LOG_TO_FILE=true
# LOG_TO_CONSOLE=true

# Monitoring (optional)
# SENTRY_DSN=your-sentry-dsn
