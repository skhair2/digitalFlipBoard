# Digital FlipBoard Server Environment Variables
# SECURITY CRITICAL: Do NOT commit the actual .env file to version control
# Copy this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=production
PORT=3001

# Supabase Configuration (required for auth)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Resend Email API Key (PRIVATE - SERVER ONLY)
# 🔴 CRITICAL: This key must NEVER be exposed to the client
# Only the server should have access to this
RESEND_API_KEY=re_your_resend_api_key

# CORS Configuration
# Only the frontend domain(s) should be listed here
# Production: https://flipdisplay.online,https://www.flipdisplay.online
# Development: http://localhost:5173,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate Limiting Configuration
RATE_LIMIT_MAX_REQUESTS=10
RATE_LIMIT_WINDOW_MS=60000

# Logging
LOG_LEVEL=info

# Monitoring (optional)
SENTRY_DSN=your-sentry-dsn
